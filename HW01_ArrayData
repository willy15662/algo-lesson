Rank 37
找array的最大值和最小值 直接掃過去O(N)

```java
  public int max() {
        int maxnum = Integer.MIN_VALUE;
        for (int i : A) {
            if (maxnum < i)
                maxnum = i;
        }
        return maxnum;
    }

    public int min() {
        int minnum = Integer.MAX_VALUE;
        for (int i : A) {
            if (minnum > i)
                minnum = i;
        }
        return minnum;
    }
```
